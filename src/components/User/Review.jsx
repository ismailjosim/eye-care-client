import React from 'react';
import useTitle from '../../hooks/useTitle';

const Review = () => {
    useTitle('Review')
    return (
        <div>
            Your Review
        </div>
    );
};

export default Review;
