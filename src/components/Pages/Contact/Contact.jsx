import React from 'react';
import useTitle from '../../../hooks/useTitle';

const Contact = () => {
    useTitle('Contact Us')
    return (
        <div>
            contact with use
        </div>
    );
};

export default Contact;
